Chartkick.LineChart("chart-1", <%= @chart_data.to_json.html_safe %>)
var enabled = <%= @tracking.cat && @tracking.sbcat && @tracking.typ ? true : false %>

$('.tracking-form .panel-heading').toggle(!enabled);
$('.tracking-form .panel-body').toggleClass('blocked', !enabled);